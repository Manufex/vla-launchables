# X-VLA training configuration

dataset:
  repo_id: "Zasha01/cube_transfer_final"

policy:
  type: "xvla"

output_dir: "/workspace/outputs/xvla"
job_name: "xvla_cube_transfer_final"

steps: 3000
batch_size: 8
device: "cuda"
dtype: "auto"  # Will be auto-detected

wandb:
  enable: true

